**Setup Instructions for INSTALL.md:**
```
# TSG Bot Installation Guide

## Prerequisites
- Azure subscription
- .NET Core SDK installed

1. Clone the repository from GitHub:
   ```
   ```
2. git clone (https://github.com/manikotony/tsg-automation.git)
   ```
   ```
3. Install the required NuGet packages:
   ```
  ```
   
4. Create a virtual environment 
   ```
  ```
5. Set up Azure resources on the Azure Portal (Azure OpenAi, Azure WorkSpaces, etc.) using the Azure portal.
6. Deploy the bot to Azure using Azure DevOps pipelines or GitHub Actions.

## Running the Bot
- Start the bot locally using Visual Studio Code or deploy it to Azure and test it using the Bot Framework Emulator.
